# match-diff

**Package:** `ichiran/characters`  
**Source:** `characters.lisp:347`  
**Definition form:** `defun`

## Inputs

`(ichiran/characters::s1 ichiran/characters::s2 &aux
  (ichiran/characters::l1 (length ichiran/characters::s1))
  (ichiran/characters::l2 (length ichiran/characters::s2)))`

## Outputs

Declared ftype: `(function (t t) (values (or cons boolean) &optional t))`

## Dependencies (ichiran symbols)

- `ichiran/characters:match-diff`
