# Audit divergences

Auto-generated by `python3 reverse/scripts/query.py audit-signatures`.
Commit alongside port files; see CONVENTIONS §7.

Each entry is a Rust port whose `pub fn` surface differs from the
captured Lisp lambda list. New entries should be either:

- intentional, citing a CONVENTIONS section (e.g. §4.4 enum collapse,
  §4.6 dropped `:fresh`); or
- a bug in the port to fix.

## Summary

- Checked: 87 fn/gf
- Macros (file-existence only): 0
- Divergences: 12

## Divergences

### `ichiran/characters:geminate`

- file: `kaniran-core/src/characters/geminate.rs`
- drift: arity 1 ≠ Lisp 2 (req=1, opt=0, keys=['fresh'])

### `ichiran/characters:get-char-class`

- file: `kaniran-core/src/characters/get_char_class.rs`
- drift: return-arity 1 inside Option ≠ Lisp (values …) required=2 (rust='Option<KanaClass>', lisp='(values t boolean &optional)')

### `ichiran/characters:join`

- file: `kaniran-core/src/characters/join.rs`
- drift: arity 2 ≠ Lisp 3 (req=2, opt=0, keys=['key'])

### `ichiran/characters:rendaku`

- file: `kaniran-core/src/characters/rendaku.rs`
- drift: arity 2 ≠ Lisp 3 (req=1, opt=0, keys=['fresh', 'handakuten'])

### `ichiran/characters:unrendaku`

- file: `kaniran-core/src/characters/unrendaku.rs`
- drift: arity 1 ≠ Lisp 2 (req=1, opt=0, keys=['fresh'])

### `ichiran/characters:voice-char`

- file: `kaniran-core/src/characters/voice_char.rs`
- drift: return-arity 1 ≠ Lisp (values …) required=2 (rust='KanaClass', lisp='(values t boolean &optional)')

### `ichiran/dict:find-word-conj-of`

- file: `kaniran-core/src/dict/find_word_conj_of.rs`
- drift: arity 3 ≠ Lisp 1 (req=1, opt=0, keys=[]) (ctx-injected; +1 absorbed)

### `ichiran/dict:find-word-seq`

- file: `kaniran-core/src/dict/find_word_seq.rs`
- drift: arity 3 ≠ Lisp 1 (req=1, opt=0, keys=[]) (ctx-injected; +1 absorbed)

### `ichiran/dict:init-suffixes-thread`

- file: `kaniran-core/src/dict/init_suffixes_thread.rs`
- drift: no `pub [async] fn init_suffixes_thread` (found: ['build_suffix_caches'])

### `ichiran/kanji:match-readings*`

- file: `kaniran-core/src/kanji/match_readings_star_.rs`
- drift: no `pub [async] fn match_readings_star_` (found: ['match_readings_star'])

### `ichiran/numbers:group-to-kana`

- file: `kaniran-core/src/numbers/group_to_kana.rs`
- drift: arity 3 ≠ Lisp 2 (req=1, opt=0, keys=['class-to-kana'])

### `ichiran/numbers:parse-number*`

- file: `kaniran-core/src/numbers/parse_number_star_.rs`
- drift: arity 1 ≠ Lisp 3 (req=1, opt=0, keys=['start', 'end'])
