# *init-suffixes-lock* (global variable)

**Package:** `ichiran/dict`  
**Source:** `dict-grammar.lisp:163`  
**Type of value:** `mutex`

## Value

```lisp
#<SB-THREAD:MUTEX "init-suffixes-lock" free owner=0>
```

_(value is not round-trippable via `read` — likely contains closures, classes, or other unreadable shapes)_
